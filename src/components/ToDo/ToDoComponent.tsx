import type { Session } from "@/lib/auth-client";


interface ProvidersProps {
    session: Session | null;
}

export default function ToDoComponent({ session }: ProvidersProps) {
    return (
        <>
        
        </>
    )
}